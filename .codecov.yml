ignore:
  - "examples/**"

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 75%
        threshold: 5%

comment:
  layout: "reach, diff, flags, files"
  require_changes: true
